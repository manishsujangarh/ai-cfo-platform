[project]
name = "ai-cfo-platform"
version = "0.1.0"
description = "Production-grade AI CFO Platform"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.18.5",
]

[tool.uv.workspace]
members = [
    "apps/api",
]
